public enum OrderType
{
    /// <summary>
    /// Enforces total (sequential) ordering.
    /// </summary>
    Total,

    /// <summary>
    /// Indicates parallel execution is permitted.
    /// </summary>
    Parallel
}
